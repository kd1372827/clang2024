#include<stdio.h>
main() {
	int o, p, q, g = 0;
	char c;
	printf("ˆ—‚ð“ü—Í");
	scanf("%c", &c);
	printf("®”‚ð“ü—Í");
	scanf("%d %d %d", o, p, q);
	switch (c) {
		if (c >= 0x5A && c <= 0x42) {
			c + 0x20;
	case 'd':printf("Å‘å’l‚Í %d ‚Å‚·");
		break;

	case 's':printf("Å¬’l‚Í %d ‚Å‚·");
		break;

	case 'g':g = o + p + q;
		printf("‡Œv‚Í %d ‚Å‚·", g);
		break;

	case 'h':g = o + p + q;
		printf("•½‹Ï‚Í %.2f ‚Å‚·", (float)g / 3);
		break;
		}
	}
}